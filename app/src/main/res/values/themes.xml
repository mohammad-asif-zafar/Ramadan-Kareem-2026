<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.NoorMuslimCompanion" parent="android:Theme.Material.Light.NoActionBar" >

    </style>

    <!-- App Theme (POST splash) -->
    <style name="Theme.NoorMuslimCompanion.Main"  parent="android:Theme.Material.Light.NoActionBar">
        <!-- Compose controls UI -->
    </style>

    <style name="Theme.NoorMuslimCompanion.Splash" parent="Theme.SplashScreen">
        <!-- Solid background -->
        <item name="windowSplashScreenBackground">@color/ramadan_green</item>

        <!-- MUST be launcher icon -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>

        <!-- Icon background -->
        <item name="windowSplashScreenIconBackgroundColor">@color/ramadan_gold</item>

        <!-- Smooth exit -->
        <item name="windowSplashScreenAnimationDuration">200</item>

        <item name="postSplashScreenTheme">@style/Theme.NoorMuslimCompanion.Main</item>
    </style>
</resources>